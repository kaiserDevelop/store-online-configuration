categories:
  property: Categories Profile Dev
  application:
    name: categories-service

server:
  port: 0

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uui}

app:
  testProp: Test propertie updated and adding more text